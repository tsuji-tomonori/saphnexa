# Aurora DSQL migration profile.
#
# Aurora DSQL requires DDL and DML in separate transactions and permits only one
# DDL statement per transaction. This PR keeps multi-DDL Flyway files for
# reviewability, so the DSQL execution profile must run migration statements
# outside a single wrapping transaction.
flyway.executeInTransaction=false
